{
  "name": "@primate/oracledb",
  "version": "0.4.0-pre",
  "description": "Oracle database for Primate",
  "homepage": "https://primate.run/docs/database/oracledb",
  "bugs": "https://github.com/primate-run/primate/issues",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/primate-run/primate",
    "directory": "packages/oracledb"
  },
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts",
    "!/**/*.spec.*"
  ],
  "scripts": {
    "build": "pnpm run clean && tsc",
    "clean": "rm -rf ./lib",
    "lint": "eslint .",
    "prepublishOnly": "pnpm run build",
    "test": "pnpm run db && npx proby",
    "db": "scripts/db.sh"
  },
  "devDependencies": {
    "@rcompat/type": "^0.18.0",
    "@types/oracledb": "^7.0.3"
  },
  "dependencies": {
    "@primate/core": "workspace:^",
    "@rcompat/assert": "^0.14.0",
    "@rcompat/is": "^0.12.1",
    "oracledb": "^7.0.1",
    "pema": "workspace:^"
  },
  "peerDependencies": {
    "primate": "workspace:^"
  },
  "imports": {
    "#*": {
      "@primate/source": "./src/private/*.ts",
      "default": "./lib/private/*.js"
    }
  },
  "exports": {
    ".": {
      "@primate/source": "./src/public/index.ts",
      "default": "./lib/public/index.js"
    }
  }
}
