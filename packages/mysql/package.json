{
  "name": "@primate/mysql",
  "version": "0.10.0-pre",
  "description": "MySQL for Primate",
  "homepage": "https://primate.run/docs/database/mysql",
  "bugs": "https://github.com/primate-run/primate/issues",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/primate-run/primate",
    "directory": "packages/mysql"
  },
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts",
    "!/**/*.spec.*"
  ],
  "scripts": {
    "build": "pnpm run clean && tsc",
    "clean": "rm -rf ./lib",
    "lint": "eslint .",
    "prepublishOnly": "pnpm run build",
    "mysql": "scripts/mysql.sh",
    "mariadb": "scripts/mariadb.sh",
    "test:mysql": "pnpm run mysql && npx proby",
    "test:mariadb": "pnpm run mariadb && npx proby",
    "test": "pnpm run test:mysql"
  },
  "devDependencies": {
    "@rcompat/type": "^0.18.0"
  },
  "dependencies": {
    "@primate/core": "workspace:^",
    "@rcompat/assert": "^0.14.0",
    "@rcompat/is": "^0.12.1",
    "mysql2": "^3.24.3",
    "pema": "workspace:^"
  },
  "peerDependencies": {
    "primate": "workspace:^"
  },
  "imports": {
    "#*": {
      "@primate/source": "./src/private/*.ts",
      "default": "./lib/private/*.js"
    }
  },
  "exports": {
    ".": {
      "@primate/source": "./src/public/index.ts",
      "default": "./lib/public/index.js"
    }
  }
}
